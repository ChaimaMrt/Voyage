package tn.esprit.pidev.services;

import tn.esprit.pidev.entities.Publication;
import tn.esprit.pidev.entities.Voyage;

public interface IPublicationService {

	Long ModifierPublication(Publication publication);
	Long AjouterPublication(Publication publication);
	void SupprimerPublication(Long idPublication);
	Publication getPublicationById(Long idPublication);
}
